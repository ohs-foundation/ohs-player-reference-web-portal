{
  "name": "ohs-player-reference-web-portal",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "seed": "tsx scripts/seed.ts",
    "bundle:check": "node scripts/check-core-bundle-size.mjs",
    "contrast:check": "node scripts/check-contrast.mjs",
    "clean": "turbo run clean && rm -rf node_modules",
    "scheme:generate": "tsx scripts/generate-scheme.ts"
  },
  "devDependencies": {
    "@material/material-color-utilities": "^0.4.0",
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
