import * as Dialog from '@radix-ui/react-dialog';
import * as Toast from '@radix-ui/react-toast';
import * as Tooltip from '@radix-ui/react-tooltip';

/** Radix Dialog — Material Web `md-dialog` migration tracked separately. */
export const OhsDialog = Dialog;

/** Radix Toast — no Material Web snackbar equivalent in `@material/web`. */
export const OhsToast = Toast;

/** Radix Tooltip — no Material Web tooltip equivalent in `@material/web`. */
export const OhsTooltip = Tooltip;
